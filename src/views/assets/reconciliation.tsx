import { SpaceView } from '@/components/space-view';
import { defineComponent } from 'vue';

export default defineComponent({
  name: 'AssetsReconciliation',

  setup () {

  },

  render () {
    return (
      <SpaceView>
        对账单
      </SpaceView>
    );
  }
});
